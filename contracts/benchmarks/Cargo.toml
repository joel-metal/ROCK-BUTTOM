[package]
name = "crowdfund-benchmarks"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
crowdfund = { path = "../crowdfund" }
soroban-sdk = { workspace = true }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "contract_benchmarks"
harness = false
